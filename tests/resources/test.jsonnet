// local x = 2 + 3;
// local y = x + 4;
// y + '_hej'

// local makeArr = function() (
//   local secret = 42;
//   [secret]
// );

// local myArr = makeArr();
// local poison = 999;
// myArr[0]

local asdf = function(x, y=100, z=101) [1, x, y, z];
asdf(1, z=2)
